server:
  shutdown: graceful
  port: 8080

spring:
  config:
    import:
      - application-db.yml
      - application-security.yml
      - application-mail.yml
      - application-s3.yml
      - application-file.yml

file:
  dir: classpath:files/